{"code":"class Drag {\r\n    constructor() {\r\n        this.x = 0;\r\n        this.y = 0;\r\n        this.c = 0.0000001;\r\n    }\r\n    setIntensity(n) {\r\n        this.c *= n;\r\n    }\r\n    setCenter(x, y) {\r\n        this.x = x;\r\n        this.y = y;\r\n    }\r\n    update(_, x, y, data, offset) {\r\n        const dx = this.x - x;\r\n        const dy = this.y - y;\r\n        const n = dx ** 2 + dy ** 2;\r\n        const fx = Math.sign(dx) * this.c * n / (dx * Math.sqrt(n));\r\n        const fy = Math.sign(dy) * this.c * n / (dy * Math.sqrt(n));\r\n        data[offset + 2] += fx;\r\n        data[offset + 3] += fy;\r\n    }\r\n}\r\nexport default Drag;\r\n"}
